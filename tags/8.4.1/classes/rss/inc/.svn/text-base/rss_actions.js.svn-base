function remove_feed(id) {
	if (id) {
		if (confirm(gettext("Are you sure you want to delete this feed?"))) {
			url = 'index.php?mod=rss&action=removeFeed&id='+id;
			document.location.href=url;
		}
	}
}
