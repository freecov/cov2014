function save_cost(id) {
	document.getElementById('cost_'+id).submit();
}

function remove_cost(id) {
	if (confirm(gettext("Are you sure you want to delete this entry?"))) {
		document.getElementById('cost_'+id).subaction.value='delete';
		document.getElementById('cost_'+id).submit();
	}
}
